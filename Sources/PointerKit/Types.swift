public enum CopyElementType {
  case sourceElement
  case destinationElement
}

public enum CopySourceElementType {
  case sourceElement
}

public enum CopyDestinationElementType {
  case destinationElement
}

